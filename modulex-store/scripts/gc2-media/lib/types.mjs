export const ELIGIBLE_ACTIONS = new Set(["adapt", "parent_attributed"]);
export const MAX_SOURCE_BYTES = 20 * 1024 * 1024;
export const OPTIMIZED_WEBP_QUALITY = 80;
export const TARGET_LONG_EDGE = Object.freeze({
  showroom: 2560,
  project: 1920,
  general: 1600,
});

export const ALLOWED_SOURCE_HOSTS = new Set([
  "granitecenterva.com",
  "www.granitecenterva.com",
]);
